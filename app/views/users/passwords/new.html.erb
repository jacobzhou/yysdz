<h2>&nbsp;</h2>
<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <div class="inputs">
    <%= f.input :email, :required => true %>
  </div>

  <div class="actions">
    <%= f.button :submit, "发送重置密码邮件", :class => 'btn_2k3' %>
  </div>
<% end %>

<%#= render :partial => "devise/shared/links" %>